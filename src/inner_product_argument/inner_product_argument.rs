// Inner Product Agrument (IPA)

// Reduction step
// m = N/2
// Z(L) = amb0 + am+1.b1 + ... + an-1bm-1 = a(R) . b(L)
// Z(R) = a0bm + a1bm+1 + ... + am-1bn-1 = a(L) . b(R)

// Prover computes commitment
// C(L) =
// C(R) =
